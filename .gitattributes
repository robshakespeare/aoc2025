# Set the default behavior so that Git will always convert line endings in text files to match local OS on checkout.
* text=auto

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.dll binary
*.exe binary
